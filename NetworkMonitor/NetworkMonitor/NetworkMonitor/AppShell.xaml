<?xml version="1.0" encoding="utf-8" ?>
<Shell xmlns="http://xamarin.com/schemas/2014/forms"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       x:Class="NetworkMonitor.AppShell"
       xmlns:pages="clr-namespace:NetworkMonitor.Views">
    <Shell.Resources>
        <Style x:Key="BaseStyle"
               TargetType="Element">
            <Setter Property="Shell.ForegroundColor"
                    Value="White" />
            <Setter Property="Shell.TitleColor"
                    Value="White" />
            <Setter Property="Shell.DisabledColor"
                    Value="#B4FFFFFF" />
            <Setter Property="Shell.UnselectedColor"
                    Value="#95FFFFFF" />
        </Style>
        <Style TargetType="ShellItem"
               BasedOn="{StaticResource BaseStyle}" />
    </Shell.Resources>

    <ShellContent Title="Текущая статистика" ContentTemplate="{DataTemplate pages:NetMonitorPage}"/>
    <ShellContent Title="Информация о подключении" ContentTemplate="{DataTemplate pages:ConnectionInfoPage}"/>
    <ShellContent Title="История подключений" ContentTemplate="{DataTemplate pages:ConnectionHistoryPage}"/>

</Shell>